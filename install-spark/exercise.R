#!/usr/bin/env Rscript
### This is not really an exercise but a test: can you install spark and get it to run?
### Try to perform the following steps:

createData <- function(n) {
   ## create a large file of artificial data and save on disk
   users <- 
}
